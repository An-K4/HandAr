<?xml version="1.0" encoding="utf-8"?>
<!-- top bar dùng chung cho màn quay (fragment_camera_record) và màn xem trước effect (fragment_effect_preview). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <!-- fitStart: icon giữ nguyên kích thước gốc, dính mép trái; cả ô weight 1 là vùng bấm -->
    <ImageButton
        android:id="@+id/btn_back"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:background="@null"
        android:contentDescription="@string/back"
        android:scaleType="fitStart"
        android:src="@drawable/ic_back" />

    <TextView
        android:id="@+id/text_effect_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="4"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:shadowColor="@color/black"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="4"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold" />

    <Space
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_weight="1" />
</LinearLayout>
